[project]
name = "project-3-2026"
version = "0.1.0"
description = "最优化理论 Project 3 (2026) — optimization algorithms on the iris and MNIST datasets"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.9",
    "numpy>=2.4.6",
    "scipy>=1.17.1",
]
