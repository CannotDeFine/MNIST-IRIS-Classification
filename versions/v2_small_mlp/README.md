# V2: Small MLP

This version adds a small hidden ReLU layer to check whether nonlinear representation helps.

Run:

```bash
uv run python versions/v2_small_mlp/run.py --dataset all --profile quick
```

Outputs:

```text
results/versions/v2_small_mlp/quick.json
```
