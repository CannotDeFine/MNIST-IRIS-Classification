# V0: Untrained Random Network

This version initializes the current MLP architecture and records metrics at `epoch 000` without gradient updates.

Run:

```bash
uv run python versions/v0_untrained_random/run.py --dataset all --profile quick
```

Outputs:

```text
results/versions/v0_untrained_random/quick.json
```
