# V1: Linear Softmax Baseline

This version trains the simplest trainable classifier: one linear layer followed by softmax and cross-entropy.

Run:

```bash
uv run python versions/v1_linear_softmax/run.py --dataset all --profile quick
```

Outputs:

```text
results/versions/v1_linear_softmax/quick.json
```
